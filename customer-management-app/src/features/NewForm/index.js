import { SafeAreaView, ScrollView, Text, View, TouchableOpacity, Pressable } from 'react-native';
// import stylesFn from './styles'

const NewForm = () => {
    // const styles = stylesFn();
    return (
        <SafeAreaView>
            <ScrollView>
                <View>
                    <Text>Hello this is a new form screen</Text>
                </View>
            </ScrollView>
        </SafeAreaView>
    );
};

export default NewForm;